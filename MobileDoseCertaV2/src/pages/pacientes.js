import * as React from "react";
import { View, Text } from "react-native";

const Pacientes = () => {
  return (
    <View>
      <Text>Pacientes</Text>
    </View>
  );
};

export default Pacientes;
