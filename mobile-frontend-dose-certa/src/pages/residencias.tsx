import * as React from "react";
import { View, Text } from "react-native";

const Residencias = () => {
  return (
    <View>
      <Text>Residencias</Text>
    </View>
  );
};

export default Residencias;
