/* GENERATED FILE */
import React, { useContext, useMemo } from 'react';
import { IconContext } from '../lib';
import bold from '../bold/MicrosoftOutlookLogo';
import duotone from '../duotone/MicrosoftOutlookLogo';
import fill from '../fill/MicrosoftOutlookLogo';
import light from '../light/MicrosoftOutlookLogo';
import regular from '../regular/MicrosoftOutlookLogo';
import thin from '../thin/MicrosoftOutlookLogo';
function MicrosoftOutlookLogo(_ref) {
  let {
    weight,
    color,
    size,
    style,
    mirrored
  } = _ref;
  const {
    color: contextColor = '#000',
    size: contextSize = 24,
    weight: contextWeight = 'regular',
    mirrored: contextMirrored = false,
    style: contextStyle
  } = useContext(IconContext);
  const IconComponent = useMemo(() => {
    const iconWeight = weight ?? contextWeight;
    const weightMap = {
      bold,
      duotone,
      fill,
      light,
      regular,
      thin
    };
    return weightMap[iconWeight];
  }, [weight, contextWeight]);
  const mirroredValue = mirrored ?? contextMirrored;
  return /*#__PURE__*/React.createElement(IconComponent, {
    color: color ?? contextColor,
    size: size ?? contextSize,
    style: [contextStyle, style, {
      ...(mirroredValue && {
        transform: [{
          scaleX: -1
        }]
      })
    }]
  });
}
export default MicrosoftOutlookLogo;
//# sourceMappingURL=MicrosoftOutlookLogo.js.map